function redraw(){
	context.clearRect(0, 0, context.canvas.width, context.canvas.height); // Clears the canvas
}
// $("#clear").click(function clearCanvas(contextReal, contextDraft){
// 	contextReal.clearRect(0, 0, 800, 400);
// 	contextDraft.clearRect(0, 0, 800, 400);
// });